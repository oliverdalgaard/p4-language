schema Transaction = {
    id: int,
    transaction: string,
    amount: int
}

table<Transaction> transactions = read("../../../MatildaCSVFiles/transaction.csv");
table<Transaction> filtered = FILTER(transactions, 2000 < amount && id == 1);
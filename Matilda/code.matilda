schema Customers = {
    customer_id : int,
    name : string,
    hej : bool
}

table<Customers> CustomersTable = read("test.csv");

print CustomersTable;

table<Customers> filteredCustomersTable = FILTER(CustomersTable, customer_id < 4 && 2 < customer_id);

print filteredCustomersTable;
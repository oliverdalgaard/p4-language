/*
schema Customers = {
    customer_id : int,
    name : string,
    hej : bool
}

function table<Customers> filterCustomers(table<Customers> customers_table) {
    return FILTER(customers_table, customer_id < 5 && 1 < customer_id);
}

table<Customers> CustomersTable = read("test.csv");

table<Customers> filteredCustomersTable = filterCustomers(CustomersTable);

print filteredCustomersTable;
*/

schema test = {
    name: string,
    age: int
}

schema test2 = {
    age: int,
    name : string
}

function table<test2> hej(table<test> test) {
    table<test> hollo = read("dasd");
    return read("hej");
}

table<test> testTable = read("123");
table<test2> testTable2 = read("123");

testTable = FILTER(testTable2, true);